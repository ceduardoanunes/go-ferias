# Arquivo

Material histórico/de referência, fora do caminho principal do projeto —
nada aqui é necessário pra rodar ou fazer deploy do app.

- `ANALISE-TECNICA.md`, `RELATORIO-SESSAO.md` — relatórios de sessões passadas.
- `render.yaml` — blueprint de deploy no Render (usado até 09/2026; o app migrou pro servidor da empresa).
- `off-*` — protótipos/exports antigos (pasta `OFF/` original), anteriores ao `index.html` atual.
- `_print-*.png` — capturas de tela usadas em revisões de design.
